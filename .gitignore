bin/
dist/
.pg-data/
.pg-socket-path
.cognosis-project
.claude/settings.local.json
*.test
result
result-*
harness
# The example flake intentionally floats on github:enqack/cognosis + nixpkgs
# rather than pinning a lockfile a reader would need to keep in sync by hand.
contrib/flake.lock

# IDE / editor
.idea/

# Retrieval-eval sweeps rewrite these on every run — recorded measurements
# for a human to read, never diffed or asserted against (bounds are asserted in
# the tests). A committed copy is just whoever ran the sweep last.
internal/query/retrievaleval/testdata/
